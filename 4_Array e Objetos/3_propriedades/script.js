let nome = 'André';
console.log(nome.length);

let numeros = [1, 2, 3, 4, 5];
console.log(numeros.length);
console.log(numeros['length']);
console.log(numeros[1])

// Tudo que tiver que vier seguido de . (ponto) 
// depois de uma varável por exemplo
// ou até entre colchetes em um array
// é uma propriedade