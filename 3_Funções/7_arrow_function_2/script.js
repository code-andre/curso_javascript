const multiplicarPorDois = (x) => {
    return x * 2;
}

console.log(multiplicarPorDois(2));

//-----------------------------------------

const divideDoisNumero = x => x / 2;

console.log(divideDoisNumero(4));
