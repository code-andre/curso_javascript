/*Armazene em valores em variáveis com cada um dos tipos de dados
vistos*/
/*String, Number, Boolean*/

let nome = "André";
let idade = 20;
let altura = 1.85;


console.log(`${nome} (${typeof nome}), tem ${idade} (${typeof idade}) anos e ${altura} (${typeof altura}) metros de altura`);

let estudando = 3 > 2;
console.log("Está estudando? " + estudando + " (" + typeof estudando + ")")